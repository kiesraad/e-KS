name: Sigrid Publish

permissions:
  contents: read

on:
  push:
    branches:
      - main

jobs:
  sigridci:
    if: github.repository == 'kiesraad/e-ks'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Sigrid CI
        uses: Software-Improvement-Group/sigridci@399234b7999c04d51d124545b94e73694d3264e9 # main
        with:
          customer: kiesraad
          system: e-ks
          convert: rust
          publishonly: true
          exclude: deploy,playwright,validate,frontend/styles
        env:
          SIGRID_CI_TOKEN: "${{ secrets.SIGRID_CI_TOKEN }}"
