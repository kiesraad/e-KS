{{- if .Values.typst.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: typst
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: typst
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
{{- end }}
