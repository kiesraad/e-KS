apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: access-to-postgres
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              access-to-postgres: allow
  podSelector:
    matchLabels:
      app: database
  policyTypes:
    - Ingress
