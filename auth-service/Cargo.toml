[package]
name = "auth-service"
version = "0.1.0"
edition = "2024"
license = "EUPL-1.2"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
axum = { version = "0.8", features = ["macros"] }
axum-extra = { version = "0.12", features = ["cookie"] }
secrecy = "0.10.3"
