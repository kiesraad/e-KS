[package]
name = "eks-locales"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Shared locale tooling for the eks build script, test suite and update_locales"

[dependencies]
phf_codegen = { workspace = true }
regex = { workspace = true }

[dev-dependencies]
uuid = { workspace = true }

[lints]
workspace = true
